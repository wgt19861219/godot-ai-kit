# 5-archive/ — 归档

每轮开发完成后的产物快照,作为下一轮迭代的基线参考。

## 放什么

- **轮次 GDD/log 快照**:每轮 `1-design/` 的 GDD + `3-verify/` 的验证报告归档
- **demo 版本归档**:demo 的重要版本快照(对应套件版本的 demo 状态)

## 命名约定

`round-N/`(N 为轮次序号)或 `vX.Y/`(对应套件版本号)。

## 注意

归档是**只读快照**,不修改。下一轮在 `1-design/` 重新开始。
